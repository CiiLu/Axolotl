//! Legacy adaptive download engine.
//!
//! This is the engine that currently lives in `fetch.rs`. It will be
//! extracted here unchanged while the XMCL-compatible engine is built.

// The legacy implementation is still in `fetch.rs`; this module exists
// as the future home for the extracted code.
